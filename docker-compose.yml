

services:

  app:
# . は現在のディレクトリ . にあるDockerfileを使ってビルドさせる
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db



  db:
    image: postgres
  
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
