

services:

  db:
    image: postgres
    environment: 
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres
  app:

    build: .
    ports: 
      - "8000:8000"
    depends_on:
      - db
    
